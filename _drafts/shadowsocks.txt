pip 安装： pip install shadosocks 下载python 源码安装：pypi 链接
{
        "server":"my_server_ip",
        "server_port":8388,
                "local_address": "127.0.0.1",
                    "local_port":1080,
                        "password":"mypassword",
                            "timeout":300,
                                "method":"rc-md5",
                                    "fast_open": false,
                                        "workers": 100
}

启动： ssserver -c /etc/shadowsocks.json -d start
关闭： ssserver -c /etc/shadowsocks.json -d stop
/sslocal
启动客户端shadowsocks服务
启动：sslocal -c /etc/shadowsocks.json -d start
关闭：sslocal -c /etc/shadowsocks.json -d stop

